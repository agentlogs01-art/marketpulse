{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "ln -sfn /app marketpulse && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn api.app:app"
  }
}
